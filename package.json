{
  "name": "nodeassignment4",
  "version": "1.0.0",
  "description": "includes template engines (ejs) Node js with Npm init, nodemon, Express.js app which servers 2 html files and some static files like(.css external css file)",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js",
    "server_start": "node app.js"
  },
  "author": "Rahimjohn",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
